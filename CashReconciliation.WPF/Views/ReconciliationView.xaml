<UserControl x:Class="CashReconciliation.WPF.Views.ReconciliationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"             
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"     
             xmlns:views="clr-namespace:CashReconciliation.WPF.Views">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
         <xctk:DateTimeUpDown Value="{Binding ReconciledDate}" Grid.Row="0" Grid.Column="0"/>
        <views:CashEntriesView Grid.Column="0" Grid.Row="1" CashEntries="{Binding StartOfDayCashEntries}" Total="{Binding StartOfDayTotal}"/>
        <views:CashEntriesView Grid.Column="1" Grid.Row="1" CashEntries="{Binding EndOfDayCashEntries}" Total="{Binding EndOfDayTotal}"/>
    </Grid>
</UserControl>
